import 'package:flutter/material.dart';

class AppService extends StatelessWidget {
  const AppService({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
